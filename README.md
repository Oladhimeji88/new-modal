
  # Build User Understanding

  This is a code bundle for Build User Understanding. The original project is available at https://www.figma.com/design/dPgZoEtN2uBMGBI85y8gld/Build-User-Understanding.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to build and start the local preview server.

  If you need Next.js development mode specifically, run `npm run dev:next`.
  
